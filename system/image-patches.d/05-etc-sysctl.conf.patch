diff --git a/etc/sysctl.conf b/etc/sysctl.conf
--- a/etc/sysctl.conf
+++ b/etc/sysctl.conf
@@ -1,4 +1,17 @@
 #
 # Configure kernel parameters at boot.
 # See sysctl.d(5) for more details.
 #
+
+# Disable IPv6 support.
+net.ipv6.conf.all.disable_ipv6 = 1
+
+# ... but enable on lo for now.
+net.ipv6.conf.lo.disable_ipv6 = 0
+
+# XXX Disable module autoloading.
+# kernel.modprobe = /
+
+# Change ttl+=1 in case your mobile provider forbids tethering.
+# Shouldn't hurt anyway.
+net.ipv4.ip_default_ttl=65
